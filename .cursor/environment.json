{
  "name": "nir-rs",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": "rustc --version && cargo fmt --version && cargo clippy --version && pkg-config --exists hdf5 && cargo fetch",
  "terminals": [
    {
      "name": "shell",
      "command": "bash",
      "description": "Interactive shell with Rust + libhdf5 ready for cargo fmt/clippy/test --all-features"
    }
  ]
}
